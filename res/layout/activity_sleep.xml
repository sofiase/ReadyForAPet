<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SleepActivity" >

    <Button
        android:id="@+id/stopsleeping"
        android:layout_width="110dp"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="31dp"
        android:layout_marginRight="18dp"
        android:background="@drawable/buttonshape"
        android:text="@string/stop_sleeping" />

    <Button
        android:id="@+id/startsleeping"
        android:layout_width="110dp"
        android:layout_height="50dp"
        android:layout_alignBaseline="@+id/stopsleeping"
        android:layout_alignBottom="@+id/stopsleeping"
        android:layout_alignParentLeft="true"
        android:layout_marginLeft="18dp"
        android:background="@drawable/buttonshape"
        android:text="@string/start_sleeping" />

    <ImageView
        android:id="@+id/sleepingdog"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_above="@+id/stopsleeping"
        android:layout_alignRight="@+id/stopsleeping"
        android:layout_marginBottom="27dp"
        android:src="@drawable/sleepingdogv1" />

</RelativeLayout>